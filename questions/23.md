# React 输出题 23：空依赖 effect 的 cleanup 保留的是第一次的值

## 基本信息
- 分类：生命周期 / 清理阶段
- 难度：中级
- 标签：清理函数, 闭包, 卸载
- 题型：阅读 React 代码并写出输出

## 题目描述
子组件卸载时 cleanup 会打印哪个 `value`？

## 代码
```jsx
function Child({ value }) {
  React.useEffect(() => {
    return () => {
      console.log("cleanup", value);
    };
  }, []);

  return null;
}

function App() {
  const [show, setShow] = React.useState(true);
  const [value, setValue] = React.useState(0);

  React.useEffect(() => {
    setValue(10);
    setShow(false);
  }, []);

  return show ? <Child value={value} /> : null;
}

render(<App />);
```

## 参考答案
最终会打印 `cleanup 0`。

## 考察点
- 空依赖 effect 会保留挂载当时的闭包

## 详细讲解
这个 cleanup 是在第一次渲染时创建出来的，当时 `value` 还是 `0`。因为 effect 的依赖数组为空，它之后不会重新注册新的 cleanup，所以即使父组件后来把 `value` 改成了 `10`，卸载时 cleanup 里读到的仍然是旧值 `0`。
