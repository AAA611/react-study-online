# React 输出题 09：父组件重渲染不一定会让子组件重新计算 memo

## 基本信息
- 分类：性能 / useMemo
- 难度：初级
- 标签：父子组件, useMemo, 渲染
- 题型：阅读 React 代码并写出输出

## 题目描述
父组件点击后重新渲染，子组件里的 `memo` 计算会不会重新执行？

## 代码
```jsx
function Child({ label }) {
  const value = React.useMemo(() => {
    console.log("memo", label);
    return label + "!";
  }, [label]);

  console.log("child render", value);
  return null;
}

function App() {
  const [count, setCount] = React.useState(0);
  console.log("app render", count);

  return (
    <div>
      <button onClick={() => setCount(c => c + 1)}>{count}</button>
      <Child label="React" />
    </div>
  );
}

render(<App />);
```

## 参考答案
首次渲染会打印 `app render 0`、`memo React`、`child render React!`。点击一次后会打印 `app render 1` 和 `child render React!`，但不会再次打印 `memo React`。

## 考察点
- 父组件重渲染时，子组件函数本身仍可能重新执行
- 依赖稳定时，memo 的计算回调会被跳过

## 详细讲解
父组件状态变化后，子组件函数本身还是会重新执行一次，所以 `child render` 会再次出现。但 `label` 依然是同一个字符串 `React`，因此 `useMemo` 会直接返回缓存结果，不会再次执行计算函数，也就不会再次输出 `memo React`。
