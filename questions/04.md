# React 输出题 04：effect 依赖变化时的执行顺序

## 基本信息
- 分类：Hooks / useEffect
- 难度：初级
- 标签：useEffect, 依赖数组, 顺序
- 题型：阅读 React 代码并写出输出

## 题目描述
请写出组件从首次挂载到更新完成的完整输出顺序。

## 代码
```jsx
function App() {
  const [value, setValue] = React.useState(0);

  console.log("render", value);

  React.useEffect(() => {
    console.log("effect", value);
    if (value === 0) setValue(1);
  }, [value]);

  return null;
}

render(<App />);
```

## 参考答案
输出顺序是：`render 0 -> effect 0 -> render 1 -> effect 1`。

## 考察点
- 渲染一定先于 effect 执行
- 依赖变化后 effect 会重新执行

## 详细讲解
首次渲染时 `value` 为 `0`，所以先输出 `render 0`。提交完成后 `effect` 执行，输出 `effect 0`，并因为 `value === 0` 而触发 `setValue(1)`。接着组件重新渲染，输出 `render 1`。由于依赖项 `value` 发生了变化，`effect` 会再次执行，因此最后输出 `effect 1`。
