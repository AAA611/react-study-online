# React 输出题 07：useRef 可以为异步回调保存最新值

## 基本信息
- 分类：Hooks / useRef
- 难度：中级
- 标签：useRef, 闭包, 最新值
- 题型：阅读 React 代码并写出输出

## 题目描述
和上一题类似，但这次使用 `ref` 保存最新值。点击一次后，1 秒后会输出什么？

## 代码
```jsx
function App() {
  const [count, setCount] = React.useState(0);
  const latest = React.useRef(count);
  latest.current = count;

  const handleClick = () => {
    setTimeout(() => {
      console.log(latest.current);
    }, 1000);
    setCount(c => c + 1);
  };

  return <button onClick={handleClick}>{count}</button>;
}

render(<App />);
```

## 参考答案
最终打印的是 `1`。

## 考察点
- ref 是一个可变容器
- 在很多异步场景里，读取 `ref.current` 可以避免拿到旧状态

## 详细讲解
虽然定时器回调本身依然属于旧闭包，但它访问的是 `latest.current`，而 `ref` 对象本身是稳定不变的。点击后组件重新渲染，并把 `latest.current` 更新成 `1`，所以等 1 秒后回调真正执行时，读取到的是最新值 `1`。
