# React 输出题 49：从 props 初始化的 state 不会自动跟随后续 props 变化

## 基本信息
- 分类：状态 / props 初始化
- 难度：中级
- 标签：props, 初始化, 状态
- 题型：阅读 React 代码并写出输出

## 题目描述
父组件把 `value` 从 `1` 改成 `2` 之后，子组件内部 state 会自动同步吗？

## 代码
```jsx
function Child({ value }) {
  const [inner] = React.useState(value);
  console.log("child", value, inner);
  return null;
}

function App() {
  const [value, setValue] = React.useState(1);

  React.useEffect(() => {
    setValue(2);
  }, []);

  return <Child value={value} />;
}

render(<App />);
```

## 参考答案
输出顺序是：先打印 `child 1 1`，之后再打印 `child 2 1`。

## 考察点
- useState 只会在初始化时读取一次传入的 prop 值

## 详细讲解
`useState(value)` 只会在首次渲染时把 `props.value` 用作初始值。父组件后续把 `value` 更新为 `2` 时，子组件虽然拿到了新的 props，但内部 state `inner` 不会自动跟着变，因此结果是 `child 2 1`。
